# ------------------------------
# Hugo Generated Files (Critical)
# ------------------------------
# Hugo Themes (unmodified — install via hugo mod or git submodule)
/themes/
# The public/ folder contains static HTML generated by Hugo (you publish this to your site, but don’t track it in Git)
/public/
# resources/ is Hugo’s cache for processed assets (images, CSS/JS, etc.) — auto-generated, no need to track
/resources/
# hugo_stats.json is auto-generated stats about your site (unused for your personal posts)
/hugo_stats.json

# ------------------------------
# Hugo Cache/ Temporary Files
# ------------------------------
# .hugo_build.lock prevents concurrent Hugo builds — temporary lock file
.hugo_build.lock
# Local cache for Hugo modules (if you use Hugo Modules)
/hugo_modules/

# ------------------------------
# OS/Editor Temporary Files (Universal)
# ------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN
*.cab
*.msi
*.msix
*.msm
*.msp
# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ------------------------------
# Code Editor/IDE Files (Optional but Recommended)
# ------------------------------
# VS Code (most common for Hugo/Markdown)
.vscode/
.idea/          # JetBrains (IntelliJ, GoLand, etc.)
*.sublime-project
*.sublime-workspace
*.swp           # Vim swap files
*.bak           # Backup files (e.g., post.md.bak)

# ------------------------------
# Dependency/ Package Manager Files (If Applicable)
# ------------------------------
# If you use npm/yarn for frontend assets (e.g., custom CSS/JS)
node_modules/
package-lock.json
yarn.lock
# If you use Go modules (rare for basic Hugo blogs)
go.mod
go.sum

# ------------------------------
# Personal/ Environment Files (Avoid Leaking Sensitive Data)
# ------------------------------
# Environment variables (e.g., API keys for Hugo integrations — never track these!)
.env
.env.local
.env.production
# Log files
*.log
